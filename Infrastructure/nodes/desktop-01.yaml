# Proxmox host — described, not provisioned via API (it hosts the LXCs/VMs).
# Specs are TODO: confirmed by Phase 2 discovery against the live cluster,
# not guessed. See ../README.md.

apiVersion: homelab/v1
kind: Node

metadata:
  name: desktop-01
  tags: [proxmox, host, gaming-pc]

spec:
  hardware: Gaming PC                # discovered 2026-05-29
  cpu: AMD Ryzen 5 3600              # 6 cores / 12 threads
  cores: 6
  memory: 16384               # MB
  pveVersion: "9.2.2"
  boot: legacy-bios
  storages: [local, local-lvm]       # local-lvm ~1.8 TB
  # Hosts VMs 1001–1003 (all stopped): Plex-VM, gaming-vm-01, gaming-vm-02
  # network: TODO — bridges / VLAN-aware config not yet captured
