---
# NAS Group Variables
#
# ⚠️ DEVELOPMENT CREDENTIALS ONLY ⚠️
# These credentials are for use with the containerized DSM in ../../.containers/dsm/
# 
# For production:
# 1. Create a separate file (e.g., group_vars/nas_prod.yml)
# 2. Encrypt it with ansible-vault: ansible-vault create group_vars/nas_prod.yml
# 3. Never commit production credentials to version control

dsm_host: "localhost:5000"
dsm_user: "dev_account"
dsm_password: "FZzRN4XCSRncu4"