# MCP secrets
.mcp.env

# Claude Code transient state (settings.json is tracked; the lock is not)
.claude/scheduled_tasks.lock

# macOS
.DS_Store

# .NET build output
[Bb]in/
[Oo]bj/
artifacts/

# Local secrets (PVE tokens etc.)
*.env
!.mcp.env.example

# Ruflo / claude-flow runtime state (config under .claude-flow/ is tracked;
# .claude-flow/.gitignore already excludes its data/logs/sessions/neural)
ruvector.db
.swarm/
